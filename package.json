{
  "name": "linkedin-explorer-server",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "deploy": "cd worker && node_modules/.bin/wrangler deploy",
    "preview": "cd worker && node_modules/.bin/wrangler dev"
  },
  "dependencies": {
    "sql.js": "^1.12.0"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
